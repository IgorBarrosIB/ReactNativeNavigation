import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    text: {
        fontSize: 20,
        color: 'white',
    },
    button: {
        alignItems: 'center',
        backgroundColor: '#00623B',
        margin: 20,
        padding: 10,
        borderRadius: 20
    }
});